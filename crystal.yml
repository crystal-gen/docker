name: docker
version: 0.1.0

author:
  name: Chris Tate
  email: chris@studiotate.com
  url: http://studiotate.com
copyright: 2015 Studio Tate

gen:
  file:
    dockerfile:
      dest: Dockerfile
  spec:
    commands:
      key: command
      type: array
    from:
      type: string
    maintainer:
      type: string
generators:
  project:
    version: latest